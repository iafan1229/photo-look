{
  "name": "photo-look",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\" \"npm run flask\"",
    "server": "cd server && npm start",
    "client": "cd frontend && npm run dev",
    "flask": "cd flask && python3 -m venv venv && source venv/bin/activate && pip3 install -r requirements.txt && python3 app.py"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
